{
  "esy": {
    "buildsInSource": true,
    "build": [
      "cc closure.c -o closure -L #{esy-libffi.lib} -lffi -I#{esy-libffi.install / 'include'}",
      "./closure"
    ]
  },
  "dependencies": {
    "esy-libffi": "*"
  }
}
